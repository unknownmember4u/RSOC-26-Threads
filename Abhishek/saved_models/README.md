# Saved Models

This directory stores serialised model files (`.pkl` / `.h5`) produced by `BaseUrbanModel.save()`.

> **Do not commit large model files to Git.** Add `*.pkl` and `*.h5` to `.gitignore`.
